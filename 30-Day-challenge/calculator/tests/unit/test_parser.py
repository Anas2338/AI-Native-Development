import pytest

# Placeholder for parser module import once it's created
# from src.calculator import parser

def test_valid_expressions():
    # These tests will initially fail because the parser is not implemented yet
    # assert parser.parse("5+3") == ['5', '+', '3']
    # assert parser.parse("10-4") == ['10', '-', '4']
    # assert parser.parse("6*7") == ['6', '*', '7']
    # assert parser.parse("20/5") == ['20', '/', '5']
    pass

def test_invalid_expressions():
    # These tests will initially fail because the parser is not implemented yet
    # with pytest.raises(ValueError, match="Invalid expression"): # Or a more specific error
    #     parser.parse("2 + apple")
    # with pytest.raises(ValueError, match="Invalid character"): # Or a more specific error
    #     parser.parse("5^2")
    pass
